`use strict`;
/*  LANDING PAGE
        home(link home) movies (link Page)
        title 
        subtitle
        footer

        <<<< vamos a mostrar un loading >>>>
        
    MOVIES PAGE
        home(link home) movies (link Page)
        films of start words
        footer
*/

function main() {
    var entryPoint = '/';

    function generateLayout() {
        generatrHeader();

        generateFooter();
        activateRouter();
    }

    function generatrHeader() {

    }

    function generateFooter() {

    }

    function activateRouter() {

    }

    generateLayout();
}
window.addEventListener('load', main);